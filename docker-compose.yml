version: '3.8'

services:
  modcal:
    build: .
    container_name: modcal
    ports:
      - "8080:8080"
    volumes:
      # Mount your custom config file
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
    environment:
      - TZ=America/New_York  # Set your timezone
